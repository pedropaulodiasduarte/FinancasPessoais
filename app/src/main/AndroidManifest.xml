<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.financaspessoais.financaspessoais">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Adiciona"
            android:label="@string/title_activity_adiciona"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdicionaDespesa"
            android:label="@string/title_activity_adiciona_despesa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdicionaReceita"
            android:label="@string/title_activity_adiciona_receita"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sobre"
            android:label="@string/title_activity_sobre"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>